# Example: Multiple Runners Configuration
# Deploy multiple runners for the same repository (parallel jobs)
# Use this as a template and deploy with different --name arguments

# GitHub App Authentication (same for all runners)
GITHUB_APP_ID=123456
GITHUB_APP_INSTALLATION_ID=789012
GITHUB_APP_PRIVATE_KEY_PATH=/etc/github-runner/app-key.pem

# Repository (same for all)
REPO_URL=https://github.com/myorg/myrepo
LABELS=self-hosted,linux,x64,docker,parallel

# Runner name (override with --name when deploying)
RUNNER_NAME=runner-1

# Resources (lighter - more runners per server)
CPU_LIMIT=1.0
CPU_RESERVATION=0.25
MEMORY_LIMIT=2g
MEMORY_RESERVATION=512m

# Docker enabled
ENABLE_DOCKER=true
RUNNER_VERSION=2.329.0

# Aggressive cleanup (shared resources)
CLEANUP_WORKSPACE_DAYS=3
CLEANUP_DOCKER_HOURS=12

# Deploy commands:
# ./scripts/deploy.sh --config config/examples/multi-runner.env --name runner-1
# ./scripts/deploy.sh --config config/examples/multi-runner.env --name runner-2
# ./scripts/deploy.sh --config config/examples/multi-runner.env --name runner-3
